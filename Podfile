# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'PinpointPushNotifications' do
  use_frameworks! 
  pod "AWSPinpoint", :git => 'https://github.com/aws-amplify/aws-sdk-ios.git', :branch => 'lawmicha/pinpoint-sdk'

end
